node_modules
.next
.next-build
# The database and its WAL sidecars are live customer data, never source.
data/*.db
data/*.db-*
data/backups
data/uploads
.env*.local
next-env.d.ts
data/.dev-session-secret
tsconfig.tsbuildinfo
.DS_Store

# Cloudflare Workers build output
.open-next/
.wrangler/

# Tooling credentials, never read by the app
.env.tools
